import React from 'react';

const SubtleSessionNotification = () => {
  // Temporarily disabled to prevent app crashes
  // Will be re-enabled once dependency issues are resolved
  return null;
};

export default SubtleSessionNotification;